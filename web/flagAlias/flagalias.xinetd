service web
{
  disable     = no
  socket_type = stream
  protocol    = tcp
  wait        = no
  user        = ubuntu
  group       = docker
  type        = UNLISTED	
  bind        = 0.0.0.0
  port        = 5000
  server      = /home/ubuntu/challenge/current/flagAlias/cake_chef.sh
  per_source  = 2
}
